TODO: Remove single-site projection (`site`) from solver output; keep canonical multi-site `sites` structure only.
TODO: Introduce explicit `forwardingDomains` in solver IR to model tenant isolation semantics (renderer decides realization: routers / VRFs / namespaces).
